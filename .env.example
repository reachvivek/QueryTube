# ==============================================
# YouTube Data API Configuration (REQUIRED)
# ==============================================
# Get your FREE API key from: https://console.cloud.google.com/apis/credentials
# 1. Create/select a project
# 2. Enable "YouTube Data API v3"
# 3. Create credentials (API key)
# Used for: Validating YouTube URLs and fetching video metadata
# Free tier: 10,000 quota units/day (enough for ~100 videos/day)
YOUTUBE_API_KEY=AIzaSyC-your-youtube-api-key-here

# ==============================================
# OpenAI API Configuration
# ==============================================
# Get your API key from: https://platform.openai.com/api-keys
# Used for:
#   - Whisper transcription (fallback for videos >25MB or when captions unavailable)
#   - Text embeddings (text-embedding-3-small)
#   - Q&A (if selected as provider)
OPENAI_API_KEY=sk-your-openai-api-key-here

# ==============================================
# Groq API Configuration (FREE & FAST) ⚡
# ==============================================
# Get your FREE API key from: https://console.groq.com/keys
# Used for:
#   - Whisper transcription (primary, files <25MB, free tier)
#   - Ultra-fast Q&A responses (14,400 requests/day free)
# Recommended for production use - 18x faster than GPT!
GROQ_API_KEY=gsk-your-groq-api-key-here

# ==============================================
# Claude API Configuration (OPTIONAL)
# ==============================================
# Get your API key from: https://console.anthropic.com/
# Used for: High-quality Q&A responses (optional)
ANTHROPIC_API_KEY=sk-ant-your-claude-api-key-here

# ==============================================
# Pinecone Vector Database Configuration
# ==============================================
# Get your API key from: https://app.pinecone.io
PINECONE_API_KEY=pcsk_your-pinecone-api-key-here

# Pinecone Index Name (create this in your Pinecone dashboard)
# Important: Must be configured with 1536 dimensions for text-embedding-3-small
PINECONE_INDEX=querytube

# Pinecone Environment (found in your Pinecone dashboard)
# Examples: us-east-1-aws, us-west1-gcp, etc.
PINECONE_ENVIRONMENT=us-east-1-aws

# ==============================================
# Default Settings
# ==============================================
DEFAULT_CHUNK_SIZE=90
DEFAULT_OVERLAP=10

# Default AI Provider for Q&A
# Options: "groq" (recommended, free), "openai", "claude"
DEFAULT_AI_PROVIDER=groq

# Default Model (depends on provider)
# Groq models: llama-3.3-70b-versatile, llama-3.1-8b-instant, mixtral-8x7b-32768
# OpenAI models: gpt-4o, gpt-4o-mini, gpt-3.5-turbo
# Claude models: claude-3-5-sonnet-20241022, claude-3-haiku-20240307
DEFAULT_MODEL=llama-3.3-70b-versatile

# ==============================================
# Database Configuration
# ==============================================
# SQLite for local development (simple, no setup needed)
DATABASE_URL=file:./dev.db

# For production, Vercel auto-sets this to PostgreSQL

# ==============================================
# NextAuth Configuration
# ==============================================
# NextAuth Secret - Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-nextauth-secret-here

# NextAuth URL - Your app's base URL
NEXTAUTH_URL=http://localhost:3000

# ==============================================
# Email Configuration (for Magic Links)
# ==============================================
# SMTP Host (e.g., smtp.gmail.com for Gmail)
EMAIL_SERVER_HOST=smtp.gmail.com

# SMTP Port (usually 587 for TLS or 465 for SSL)
EMAIL_SERVER_PORT=587

# SMTP Username (usually your email address)
EMAIL_SERVER_USER=your-email@gmail.com

# SMTP Password (for Gmail, use App Password: https://support.google.com/accounts/answer/185833)
EMAIL_SERVER_PASSWORD=your-app-password-here

# From Email Address (what users see as sender)
EMAIL_FROM=QueryTube <noreply@querytube.app>
